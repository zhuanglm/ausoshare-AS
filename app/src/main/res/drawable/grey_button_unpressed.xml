<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- fill color -->
    <solid android:color="#6C6C6C"></solid>
    <!-- line's width -->
    <stroke
        android:width="1dp"
        android:color="#63a219"></stroke>
    <!-- rectangle radius -->
    <corners
        android:topLeftRadius="7dp"
        android:topRightRadius="7dp"
        android:bottomLeftRadius="7dp"
        android:bottomRightRadius="7dp"/>
    <!-- gradual change -->
    <gradient android:startColor="@color/grey_color_unpressed"
        android:endColor="@color/grey_color_unpressed"
        android:angle="270"/>
    <stroke
        android:width="1dp"
        android:color="@color/grey_color_unpressed" />
</shape>
